<header>
  <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <div class='container'>
      <div class="row col-12">
        <div class="col d-flex align-items-center">
          <div><a class="navbar-brand" href="/">Главная</a></div>
          <div><button class="navbar-toggler" type="button" data-bs-toggle="collapse"
              data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false"
              aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
          </div>
          <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav m-auto mb-2 mb-md-0">

              <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to 'Личный кабинет', user_path(current_user), class: "nav-link active", 
                                                            aria: { label: "page" } %>
              </li>
              <li class="nav-item">
                <%= link_to current_user.full_name, edit_user_registration_path, class: "nav-link active", 
                                                            aria: { label: "page" } %>
              </li>              
              <li class="nav-item">
                <%= link_to "Выйти", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, 
                                                               class: "nav-link active", 
                                                               aria: { label: "page" } %>
                                                               
              </li>
              <% else %>
              <li class="nav-item">
                <%= link_to "Зарегистрироваться", new_user_registration_path, class: "nav-link active", aria: { label: "page" } %>
              </li>
              <li class="nav-item">
                <%= link_to "Войти", new_user_session_path, class: "nav-link active", aria: { label: "page" } %>
              </li>
              <% end %>
            </ul>
          </div>

        </div>
        <div class='col'>
          <%= render 'partials/alert' %>
        </div>
      </div>
    </div>
  </nav>
</header>
